#include<stdio.h>

intmain()
{
    char str[10];
    char str[10];

    printf("enter the first name");
    scanf("%s\n",&str);

    printf("enter the second name");

    gets(str1);
    printf("The output is");
    puts(str);
    puts(str1);


    return 0;
}
